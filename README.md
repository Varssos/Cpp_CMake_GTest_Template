# Cpp_CMake_GTest_Template


# Build

`{build_dir}` = `build`

```
cmake -B build -DCMAKE_BUILD_TYPE=Debug -DTESTS=True
cmake --build build
sudo cmake --install build
```

# Run app 
```
./build/app
```

# Run tests
```
./build/tests
```

